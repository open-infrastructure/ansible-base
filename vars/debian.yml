---

base__system_packages:
  - tmux
  - zsh
  - mosh
  - socat
  - rsync
  - vim
  - curl
  - wget
  - tree
  - tcpdump
  - dnsutils
  - ifupdown2
  - nmap
  - git
  - python3
  - python3-apt
  - python3-pip
  - python3-virtualenv
  - python-mako  # required by ifupdown but not installed as dependency
  - python-gvgen  # required by ifupdown but not installed as dependency
  - graphviz
